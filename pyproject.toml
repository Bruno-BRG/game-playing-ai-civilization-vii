[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "civilization-vii-ai"
version = "0.1.0"
description = "A structured, observable Civilization VII AI companion"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [
  { name = "Bruno-BRG" },
]
dependencies = [
  "pillow>=11.3,<13",
]

[project.optional-dependencies]
vision = [
  "ultralytics>=8.4.0,<9",
]
windows = [
  "dxcam>=0.0.5,<1; sys_platform == 'win32'",
  "pydirectinput>=1.0.4,<2; sys_platform == 'win32'",
]

[project.scripts]
civ7-ai = "civ7_ai.cli:main"

[dependency-groups]
dev = [
  "mypy>=1.17,<2",
  "pytest>=8.4,<10",
  "ruff>=0.12,<1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/civ7_ai"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["civ7_ai"]

[[tool.mypy.overrides]]
module = ["dxcam", "pydirectinput", "ultralytics", "ultralytics.*"]
follow_imports = "skip"
ignore_missing_imports = true
